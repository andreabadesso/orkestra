# =============================================================================
# Orkestra Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# Values shown are defaults - uncomment and modify to override.
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# Connection details for the PostgreSQL database.
# Default: Uses Docker Compose PostgreSQL service

# POSTGRES_USER=orkestra
# POSTGRES_PASSWORD=orkestra_dev
# POSTGRES_DB=orkestra_dev
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# Full connection string (constructed from above values by default)
# DATABASE_URL=postgresql://orkestra:orkestra_dev@localhost:5432/orkestra_dev

# -----------------------------------------------------------------------------
# Temporal Workflow Engine
# -----------------------------------------------------------------------------
# Connection details for Temporal server.
# Default: Uses Docker Compose Temporal service

# Temporal gRPC endpoint
# TEMPORAL_ADDRESS=localhost:7233

# Temporal namespace for Orkestra workflows
# TEMPORAL_NAMESPACE=default

# Temporal Web UI port (for local development)
# TEMPORAL_UI_PORT=8080

# Temporal gRPC port
# TEMPORAL_PORT=7233

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Node environment
# NODE_ENV=development

# API server port
# API_PORT=3001

# Dashboard port
# DASHBOARD_PORT=3000

# Log level: debug, info, warn, error
# LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Multi-tenancy
# -----------------------------------------------------------------------------
# Default tenant for development

# DEFAULT_TENANT_ID=dev-tenant

# -----------------------------------------------------------------------------
# MCP Server
# -----------------------------------------------------------------------------
# Model Context Protocol server configuration

# MCP server transport: stdio, http
# MCP_TRANSPORT=stdio

# MCP HTTP port (when using http transport)
# MCP_PORT=3002

# -----------------------------------------------------------------------------
# Observability (Optional)
# -----------------------------------------------------------------------------
# Langfuse integration for AI observability

# Enable Langfuse integration
# LANGFUSE_ENABLED=false

# Langfuse API endpoint
# LANGFUSE_HOST=http://localhost:3333

# Langfuse credentials (get from Langfuse dashboard)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=

# -----------------------------------------------------------------------------
# Security (Production)
# -----------------------------------------------------------------------------
# These should be set in production environments

# JWT secret for API authentication
# JWT_SECRET=change-me-in-production

# API key for MCP server authentication
# API_KEY=change-me-in-production

# Encryption key for sensitive data
# ENCRYPTION_KEY=change-me-in-production

# -----------------------------------------------------------------------------
# Development Overrides
# -----------------------------------------------------------------------------
# Useful when ports conflict with other services on your machine

# Use different ports to avoid conflicts
# POSTGRES_PORT=5433
# TEMPORAL_PORT=7234
# TEMPORAL_UI_PORT=8081
