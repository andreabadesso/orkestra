/**
 * Dashboard Home Page - Mission Control
 *
 * The main landing page showing task overview and statistics
 */

'use client';

import { useUser } from '@/hooks/use-user';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { trpc } from '@/lib/trpc';
import Link from 'next/link';
import {
  Activity,
  Clock,
  CheckCircle2,
  AlertTriangle,
  ArrowRight,
  Zap,
  TrendingUp,
  Loader2,
} from 'lucide-react';

/**
 * Dashboard home page component
 */
export default function DashboardPage() {
  const { user } = useUser();
  const { data: stats, isLoading } = trpc.task.stats.useQuery();

  if (isLoading) {
    return (
      <div className="flex items-center justify-center h-[50vh]">
        <div className="flex flex-col items-center gap-4">
          <Loader2 className="w-12 h-12 text-control-cyan animate-spin" />
          <p className="mono-data text-control-cyan">LOADING MISSION CONTROL...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="space-y-6 animate-fade-in">
      {/* Welcome Header */}
      <div className="space-y-2">
        <h1 className="section-heading">
          Welcome back, <span className="text-control-cyan">{user?.name || 'Operator'}</span>
        </h1>
        <p className="mono-data">
          LAST LOGIN: {new Date().toLocaleString('en-US', {
            dateStyle: 'medium',
            timeStyle: 'short'
          })}
        </p>
      </div>

      {/* Statistics Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 stagger-children">
        {/* Pending Tasks */}
        <Card className="card-interactive border-control-cyan/50 hover:border-control-cyan">
          <CardHeader className="pb-2">
            <div className="flex items-center justify-between">
              <div className="w-10 h-10 rounded-lg bg-control-cyan/10 flex items-center justify-center">
                <Activity className="w-5 h-5 text-control-cyan" />
              </div>
              <Badge variant="default" className="font-mono">ACTIVE</Badge>
            </div>
          </CardHeader>
          <CardContent>
            <div className="space-y-1">
              <p className="text-4xl font-bold font-mono text-control-cyan">{stats?.pending || 0}</p>
              <p className="mono-data text-muted-foreground">PENDING TASKS</p>
            </div>
            <div className="mt-4 flex items-center gap-2 mono-data text-control-cyan">
              <TrendingUp className="w-3 h-3" />
              <span>AWAITING ASSIGNMENT</span>
            </div>
          </CardContent>
        </Card>

        {/* Claimed Tasks */}
        <Card className="card-interactive border-control-amber/50 hover:border-control-amber">
          <CardHeader className="pb-2">
            <div className="flex items-center justify-between">
              <div className="w-10 h-10 rounded-lg bg-control-amber/10 flex items-center justify-center">
                <Clock className="w-5 h-5 text-control-amber" />
              </div>
              <Badge variant="warning" className="font-mono">IN PROGRESS</Badge>
            </div>
          </CardHeader>
          <CardContent>
            <div className="space-y-1">
              <p className="text-4xl font-bold font-mono text-control-amber">{stats?.inProgress || 0}</p>
              <p className="mono-data text-muted-foreground">IN PROGRESS</p>
            </div>
            <div className="mt-4 flex items-center gap-2 mono-data text-control-amber">
              <Zap className="w-3 h-3" />
              <span>REQUIRES ACTION</span>
            </div>
          </CardContent>
        </Card>

        {/* Completed Today */}
        <Card className="card-interactive border-control-emerald/50 hover:border-control-emerald">
          <CardHeader className="pb-2">
            <div className="flex items-center justify-between">
              <div className="w-10 h-10 rounded-lg bg-control-emerald/10 flex items-center justify-center">
                <CheckCircle2 className="w-5 h-5 text-control-emerald" />
              </div>
              <Badge variant="success" className="font-mono">COMPLETED</Badge>
            </div>
          </CardHeader>
          <CardContent>
            <div className="space-y-1">
              <p className="text-4xl font-bold font-mono text-control-emerald">{stats?.completed || 0}</p>
              <p className="mono-data text-muted-foreground">TOTAL COMPLETED</p>
            </div>
            <div className="mt-4 flex items-center gap-2 mono-data text-control-emerald">
              <CheckCircle2 className="w-3 h-3" />
              <span>MISSION SUCCESS</span>
            </div>
          </CardContent>
        </Card>

        {/* Overdue */}
        <Card className="card-interactive border-control-red/50 hover:border-control-red">
          <CardHeader className="pb-2">
            <div className="flex items-center justify-between">
              <div className="w-10 h-10 rounded-lg bg-control-red/10 flex items-center justify-center">
                <AlertTriangle className="w-5 h-5 text-control-red" />
              </div>
              <Badge variant="destructive" className="font-mono">URGENT</Badge>
            </div>
          </CardHeader>
          <CardContent>
            <div className="space-y-1">
              <p className="text-4xl font-bold font-mono text-control-red">{stats?.overdue || 0}</p>
              <p className="mono-data text-muted-foreground">OVERDUE TASKS</p>
            </div>
            <div className="mt-4 flex items-center gap-2 mono-data text-control-red">
              <AlertTriangle className="w-3 h-3 animate-pulse-slow" />
              <span>IMMEDIATE ATTENTION</span>
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Quick Actions */}
      <Card className="border-control-border-bright">
        <CardHeader>
          <CardTitle className="font-serif">Quick Actions</CardTitle>
          <CardDescription className="mono-data">
            PRIMARY OPERATIONS DASHBOARD
          </CardDescription>
        </CardHeader>
        <CardContent className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <Link href="/tasks">
            <Button
              variant="outline"
              className="w-full h-24 flex flex-col items-center justify-center gap-2 hover:border-control-cyan hover:bg-control-cyan/10"
            >
              <Activity className="w-6 h-6" />
              <span className="font-mono text-sm">VIEW ALL TASKS</span>
              <ArrowRight className="w-4 h-4 opacity-50" />
            </Button>
          </Link>

          <Link href="/history">
            <Button
              variant="outline"
              className="w-full h-24 flex flex-col items-center justify-center gap-2 hover:border-control-amber hover:bg-control-amber/10"
            >
              <CheckCircle2 className="w-6 h-6" />
              <span className="font-mono text-sm">TASK HISTORY</span>
              <ArrowRight className="w-4 h-4 opacity-50" />
            </Button>
          </Link>

          <Link href="/admin/users">
            <Button
              variant="outline"
              className="w-full h-24 flex flex-col items-center justify-center gap-2 hover:border-control-emerald hover:bg-control-emerald/10"
            >
              <Activity className="w-6 h-6" />
              <span className="font-mono text-sm">MANAGE TEAM</span>
              <ArrowRight className="w-4 h-4 opacity-50" />
            </Button>
          </Link>
        </CardContent>
      </Card>

      {/* Recent Activity */}
      <Card className="border-control-border-bright">
        <CardHeader>
          <CardTitle className="font-serif">Recent Activity</CardTitle>
          <CardDescription className="mono-data">
            LATEST TASK OPERATIONS
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div className="flex flex-col items-center justify-center py-12 text-center">
            <Activity className="w-12 h-12 text-muted-foreground mb-4 opacity-50" />
            <p className="mono-data text-muted-foreground mb-2">NO RECENT ACTIVITY</p>
            <p className="text-sm text-muted-foreground max-w-md">
              Tasks will appear here when workflows require human input.
              The system is currently in standby mode.
            </p>
            <Link href="/tasks">
              <Button variant="outline" className="mt-4">
                View All Tasks
              </Button>
            </Link>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
