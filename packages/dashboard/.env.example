# NextAuth Configuration
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# API URL for backend (server-side)
ORKESTRA_API_URL=http://localhost:3001

# API URL for frontend (client-side)
NEXT_PUBLIC_API_URL=/api/trpc

# Trust proxy headers (set to true in production behind a load balancer)
AUTH_TRUST_HOST=true
